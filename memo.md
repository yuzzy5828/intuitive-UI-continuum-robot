# アーキテクチャの説明
アーキテクチャは以下の資料参考

![](img\whole_system_graph.jpg)

# システム詳細
システム全体図は以下の通り
![](img\whole_system_and_interface.jpg)

# コード詳細
今回使用しているコードはarduinoへの書き込み用コード・PCでの実行用コードに分かれている．

## PC用コード
### controller_without_interface.py
このコードでは，インタフェースを使用せず，PCと実機でのみの指令値送信を行う．
指令値を手打ちして，Modelで計算し，回転量をマイコンに送信する．

### contoller_with_interface.py
このコードではインタフェースからの受信も含めたコードになっている．in_portのインタフェース側から指令値を受信して，Modelで計算した後にout_portで送信する．

### model.py
腱駆動ロボットのモデル化による計算を行うためのModelクラスと，Serial送受信をつかさどるSerialBridgeクラスの定義を行っている．

### utils.py
根幹には関わらないが有用なコード群を実装．可視化用の関数をutils.pyに実装している．

## arduino用コード
### actuation.ino
PCからモータ駆動系への伝達をつかさどる．
PCで計算された回転量をもとに，各モータに1ステップずつ指令を送る（一つのモータごとに指令を送るわけではない．それだと形状の変化量が実機の可動域を超えて糸が切れたりモータが空回りする原因になる）

モータは200度/回転のステッピングモータを使用．

### interface.ino
インタフェース側からPCへ制御変数の指令を送る．
曲げセンサによる曲率の値と磁気エンコーダの回転をもとに，曲率一定モデルに基づいた角度指令値を送る．曲げセンサの値 $b$ に対して，Weber-Fechner則 $\theta_{ref} \propto log(b)$ が成り立つものとして関数を設計している．（ただ，曲げセンサの値自体が荒い場合は何か工夫をしたほうが良い可能性がある．）

# 今後の知見
実際に作ってみてわかったこと，また勉強しながら考えたことを書きます．

### 実際にいただいたフィードバック
- 医療の現場ではたしかにこのようなインタフェースは必要である（医療系の研究者の方）
- 模倣学習を行う・直感的なインタフェースを求めるのであれば，リーダーとフォロワーが全く同じ形であるほうが面白そう（河原塚先生より）
- 腱駆動はコストがかかってしまうので，何か別のアクチュエータでは無理なのか？
- ソフトロボットではスケールの大きさによって制御の安定さがかなり変わってしまうので，そこに対応していければもっと良くなると思う（複数）

### 自分で考えたこと
- ソフトロボットのメリットの一つは，人体との接触に都合が良いということだから，その良さを活かした「人体との接触が可能なだけでなく，適応的に制御可能なロボット」を作っていくことが目的の一つになっていくだろう．そのもとで，このセクションの7個目にかいた方法で考えていけば，体内での接触を伴うタスクでのソフトロボットの応用を考えられるかもしれない．

- ALOHAみたいな学習システムを実際に作ってみて，複雑な動きを学習させてみたら面白そう
    - もしつくるなら以下のセンサが必要：
        - リーダー：今回のインタフェースのような曲げセンサとエンコーダ
        - フォロワー：各モータに位置エンコーダ

- 今回は変形のみのシステムであるが，ここに前後移動可能な機構を加えたい．
    - アイデア一覧
        - 伸縮
            - backboneをばねにする
            - Kresling構造を用いる
            - 
        - 長さそのものを変える
            - ギアを用いたソフトロボット（今回のRobomechでの発表）

- 外側になにかで被覆をして，その表面に触覚を取り付けて制御・学習制御を行うことも考えられる
    - 触覚の利用はインタフェース側の力覚教示の研究にとどまる
        - https://ieeexplore.ieee.org/document/10910154
        - https://advanced.onlinelibrary.wiley.com/doi/full/10.1002/advs.202502353）
    - 触覚フィードバックをもちいたソフトロボットはあまり研究例が多くないから論文化できるかもしれない

- 空気圧アクチュエータという手段の考察
    - ETHのSof Robotics Labでは空気圧アクチュエータを用いて魚ロボットや象の鼻ロボットを作ったりしていて面白い

- インタフェースはもっと大きいものであるほうが望ましい．
    - 今回制作したものは小さすぎた．
    - マニピュレータなどのリジッドなロボットに比べて，ソフトロボットのインタフェースのスケールは実機よりも大きいほうがよさそうである．

- 今回の実機の設計では，曲率一定モデルをもとにした場合，これ以上のスケールでは制御に誤差が生まれる可能性が高いので，例えばばねを付け加えるなどの実機への復元力の設計が必要．例えばdisk同氏の中にばねを入れる設計にするなど．
    - これは完全に自分の中の妄想だが，ばねを実機に入れることで，モデル化の誤差の抑制・静力学モデルの考案の可能性がある．
    例えば，ばねがない状態では一番端のdiskに糸の張力が集中するためにその力のベクトルも考えにくいし，力が均等に加わらない．さらに，ロボットの位置が一意に定まらない．
    - モデル化の誤差について．ばねを設計に取り入れることで，糸の張力をばねの復元力のエネルギーに変換して解釈することができて，CCモデルで糸の長さを計算した後に「ある糸の変化量（定数）に対するばねの復元力の最小化」としてばねの変形量を定式化することで最適なロボットの形状が決定する．とくに，ばねの復元力が自然長の時に最も弾性エネルギーが低いことを考えると，論文で指摘したようなロボットのヒステリシスは起こりにくくなるはず．
    - 静力学に関して．上記の方法で運動学モデルが実機でも誤差がなく適用できれば，静力学モデルを考え始めることができる．ソフトロボットの静力学は，ラグランジアンから求めていくのがよさそうに思っている：
        - 一般化座標をCCモデルの二つのパラメタ $(\phi, \theta)$ とする．diskとばねの位置エネルギー（重力と弾性力）を考え，張力の行う仕事から一般化力 $Q$ を求めることで，Euler–Lagrange方程式を考えることができ，そこからエンドエフェクタの運動をモデル化することができそう．
        - 計測が必要なのは，糸にかかる張力・各diskのz方向の変化量・ばねの変形量）．それぞれ，トルクを計測できるモータを使用する・上記の運動学モデルでの近似計算によって推定できる．
        - これによって，手術や医療での応用ケースとして考えられる準静的なロボットのエンドエフェクタの運動を力を含めたモデルで考えられることができるはず．
        - このモデルから，エンドエフェクタの運動に対する回転量・回転速度が逆静力学によって決定されれば，制御ができる．
        - 触覚のフィードバックを適応制御の制御ゲイン変化としてみなせば，「接触に関して適応可能なロボット制御」を行うことができる．
        - 実験設定としては，例えばこのロボットのエンドエフェクタに円を描くように制御指令を送って，その間でロボットへの意図的な接触を行ったときの触覚の値の大きさ・制御ゲインの変化．実際のエンドエフェクタの軌道を記録することで，適応性を示せる．

    - ソフトロボットのCC,PCCモデルの動力学に関するモデル化については以下の論文参照．
        - https://ieeexplore.ieee.org/document/8722799

- より多彩な動きをするためには自由度を増やす必要があり，それにはCCの複数版であるPCCモデルが必要．そのようなインタフェースの機構をどのようにして作るか？
    - メカの工房の技術職員と色々話したところ，このような機構なら可能ではないか？という話になった．アイデアしかないのでここに供養．